# UX OS deferred-items decision brief

Updated 14 September 2026 after the operational-expansion release: direct Monday store intake, private onboarding-document custody, managed kiosk links, automatic store-access reconciliation, durable release work, catalog publication controls, and the policy-held communication outbox.

These items do not block the current inventory, retailer onboarding, catalog, pricing, ordering, access, kiosk, COA/lineage, financial-display, or executive-demo scope. Each related capability stays off or blank until the named owner records a decision.

## Decisions now closed

| Topic | V1 decision | Evidence |
| --- | --- | --- |
| Sellable package | Active Canix package with `status_category = 'available'`; never use the free-form status label as the decision field | Connected Canix inventory schema explicitly documents this rule |
| Reserved quantity | Subtract an explicit reservation from count-based orderable units; preserve unknown coverage as unknown | Live production data outside facility `4546` had 180 available count packages, 167,172 units, and no reservations; the one fully reserved available package was confined to the sandbox |
| Catalog grouping | One catalog product per Canix `item_id` (`canix_item_id_v1`) | 958 of 1,359 active non-sample packages had no `product_id`; only two product IDs spanned multiple item IDs |
| Quantity domains | Keep WeightBased and CountBased separate; exclude VolumeBased | Canix reporting contract and urbanXtracts policy |
| Case quantities | Normalize positive Canix `case_quantity` when supplied. Per-store enforcement defaults off and fails closed when enabled without one unambiguous value | Implemented in the protected order policy and intake contract |
| Economic owner | Portal item default with package override; may be blank; never inherit Brand | Implemented effective-dated ownership model |
| Economic partner | Automatically associate every nonblank Canix Brand with a separate partner party; never use as Economic Owner fallback | Implemented Brand-to-Partner registry and sync |
| Cost Object identity and workflow | Each valid Lot ID is explicitly Pending Finance/Ops Assignment, Assigned, or Not Required. Assigned values use `DEPT-LINE[-VARIANT]`, must exactly match the approved Monday lot row, and remain separate from Lot ID, Ownership Code, Brand, potency tier, and Canix sales-order lines. Not Required requires a note and an empty Monday Cost Object value | Protected portal decision RPC, Administrator/Operations-only permission, and append-only decision history |
| Accepted-order inventory commitment | Commit submitted orders immediately; hold through approval/processing; release on decline, cancellation, delivery, legacy exception, or definitive downstream rejection; count only the portion not covered by the explicitly linked Canix sales order | Implemented transaction-scoped per-item commitments and one-to-one Canix sales-order reconciliation; drafts still hold nothing |
| Canix snapshot publication | Stage each full fetch privately and publish packages, COAs, and the successful-run pointer in one PostgreSQL transaction | A failed or partial run cannot mutate the last successful inventory snapshot |
| Lab release quantity | Only exact `TestPassed` package units count as released; a mixed line exceeding passing units is entirely pre-order | Connected Canix snapshot status domain and fail-closed order workflow |
| Workforce SSO provider | Google Workspace through a dedicated Google Web OAuth client brokered by Supabase; first-time `@urbanxtracts.com` users receive Viewer | Google and email providers are enabled, the current callback/redirect flow was completed with Tom's Workspace account, and the deterministic provisioning trigger is deployed |
| Monday order delivery and status return | Use the dedicated Monday app for idempotent, board-pinned order/onboarding creation and status writes. Receive status changes through one app-signed webhook; retain no Make runtime fallback | The TEST order was reconciled to Monday, `Ordered` and `Approved` callbacks each processed once with HTTP 200, eight obsolete subscriptions were removed, one signed webhook remained active, and the shared-secret compatibility code was removed |
| Monday catalog-content ingestion | Pull only from pinned board `9620649212`; automatically bootstrap only unique exact product-name + Brand pairs as Draft; require an explicit current Canix Item ID and Draft, Published, or Archived state for every other row | On 2 September 2026, the controlled exact-match pass verified 17 new pairs as Draft and synchronized all 33 currently linked rows. No update failed, conflicted, duplicated a Canix ID, or referenced a missing Canix item; the immediate repeat applied zero additional mappings, confirming idempotence. Nothing was published |
| Production portal access | Expose the application sign-in screen at `portal.urbanxtracts.com`; use Supabase authentication and server-side portal permissions instead of a ChatGPT/Sites viewer gate | The production origin is the Supabase Site URL and an allowed redirect. Email/password remains available for retailer users, Google Workspace remains available for employees, and first-time workforce users receive Viewer access |
| Public onboarding anti-abuse | Require a hostname-restricted production Turnstile token plus an atomic HMAC-scoped daily limit of 3 for signed-out requests | The rotated secret is stored only in Supabase; the public site key is supplied by Sites; validation requires the exact `retailer_onboarding` action and `portal.urbanxtracts.com` hostname |
| Direct Monday store onboarding | Create one idempotent item on Store Onboarding board `18428027063`, preserve the full portal intake, and attach the submitted license document to that item | The server now owns the board write, creates its idempotency and file columns when needed, records the Monday item ID, and marks partial failures for reconciliation instead of creating a duplicate |
| Managed kiosk links | Use opaque, revocable store capabilities with an expiration, device label, optional product/category restrictions, QR output, rotation history, and de-identified usage counts | The management API and Users & Access controls enforce the lifecycle; a kiosk never receives price, internal quantity, ownership, or account data |
| New-store access reconciliation | Give active retailer Owners every qualified store automatically, leave Budtenders limited to one store, and queue Buyer assignments for Administrator review | Database trigger, backfill, protected review queue, and assign/dismiss actions are deployed |
| Durable release work register | Keep release tasks in the database with status, owner, due date, evidence, and comments while retaining separate live integration checks | Administrator and Operations manage the durable register; all other readiness viewers receive a read-only view |
| Catalog publication workflow | Compute required-content completeness and separate Draft, Review, Approved, Scheduled, Published, and Archived states | Monday can supply content but cannot cross the approval boundary; incomplete or unapproved new publish requests remain Draft, while existing published content remains stable on sync |
| QuickBooks portal boundary | Use the portal as the read-only interface for connected Customer, Invoice, Payment, and scoped retailer-account snapshots; accepted orders still go to Monday and are manually entered into QuickBooks | Production connection work was completed separately; the live readiness check remains the source of truth for connection and snapshot health |

## Controlled deferrals

| Priority | Item | Current safe behavior | Decision owner | Decision needed |
| --- | --- | --- | --- | --- |
| Controlled hold | Product-image and portal-managed COA uploads | Policy is closed: scanner required before enablement, separate reviewer required, quarantine reason required, 90-day quarantine retention, and 365-day superseded-archive retention. Upload creation fails closed while `PORTAL_ASSET_UPLOADS_ENABLED` is not `true`; Canix-supplied COA data remains available | IT / Quality | Connect and validate the approved scanner before enabling uploads. Product images remain intentionally on hold |
| Controlled hold | Onboarding-document malware scanning | New-store licenses are magic-byte validated, size-limited, hashed, archived in a private bucket, audited, and transferred to the exact Monday item. The record remains `pending_provider`; the portal does not claim a malware scan occurred. The separate retailer license-renewal uploader accepts no file and explicitly directs users to the current approved process | IT / Compliance | Select and connect the approved scanner, define quarantine notification/retention, and run clean/malicious acceptance tests before treating scan state as a release control or reopening renewal uploads |
| P0 integration | Canix Package Owner reporting bridge | The portal keeps Canix Owner separate from Economic Owner and accepts defensive REST owner shapes, but a fresh REST sync still supplied zero assignments. Canix Reporting currently supplies 474 assigned rows across 1,409 active non-sample non-volume packages; REST-backed packaged/each inventory has 1,023 blank Owner values | IT / Data / Canix | Obtain a supported server-side Reporting route/credential or a documented REST Package property, connect the five-minute sync, and verify package ID coverage plus removal behavior before closing |
| P0 before cost-object enforcement | Flower-intake Cost Object assignment and source register | The portal provides Pending, Assigned, Not Required, and Source Conflict queues at Lot grain. It never infers a code, accepts Assigned only when it exactly matches an approved Monday row, rejects Not Required while Monday contains a value, and keeps packages without a valid Lot ID in a separate exception queue. The latest audit found 65 Monday lot rows, zero approved rows, zero valid lots, and 804 packages still missing Lot ID. UX-AUX-CMP-POL-004, UX-AUX-OPS-PRC-SOP-006, and UX-AUX-FIN-SOP-011 govern receiving and accounting but do not assign the code | Amrit Kharas (Finance/Controller) / Jonathan DeMart (Operations) | Approve source rows mapping each facility/intake stream to `DEPT-LINE[-VARIANT]`, or record an evidenced Not Required decision per Lot; complete Lot IDs first |
| P0 before live pricing | Wholesale price-to-Canix crosswalk | All 118 `ACTIVE CART` rows are staged in a protected portal queue. Fifty-one active defaults are published; 67 identity reviews remain unpublished. Store-specific approved prices take precedence | Sales Operations / Data | Review the remaining identities, then publish each verified default price without touching store-specific approvals |
| Complete | Retire the paused Make intake compatibility credential | Direct Monday is the only application path for order, onboarding, and order-status writes. Legacy license and recall intake kinds fail closed, the separate license-renewal upload cannot claim delivery, both obsolete Supabase secrets were removed, and all four old filter-secret references were invalidated in the renamed inactive Make scenario | IT | Keep the retired scenario inactive as an audit artifact; any future fallback would require a new credential, security review, acceptance test, and deliberate release approval |
| Complete | Minimum order and lead time | Administrator, Operations, or Sales may set or clear a licensed-store minimum value and informational calendar-day lead time. Existing stores default to no minimum and no published lead time; the server rechecks a configured minimum at submission | Sales Operations | Maintain the values per store as commercial policy changes |
| P1 | Owner Reconciliation Center | Brand, Canix Owner, Economic Owner, and Economic Partner remain separate. Blank assignments and mismatches are visible; no value is inferred or auto-copied between them | Finance Operations / Data / Operations | Approve Economic Owner precedence and inheritance, exception routing, bulk remediation, and settlement-reporting rules |
| P1 | Margin formula and component-coverage threshold | Cost/margin stays out of navigation | Finance | Define included cost components and minimum coverage |
| P1 | Exception ownership and service levels | Status is visible; no unsupported clock is promised | Operations | Name owner and response target by exception type |
| P1 | Approval aging and escalation | Store Owner remains the sole approval holder with no automatic delegation | Sales leadership | Define escalation time and backup path |
| P1 | License review evidence and cadence | Portal records explicit qualification; it does not claim independent validity | Quality / Compliance | Name reviewer, retained evidence, and recheck cycle |
| P2 | Monday store-visit notes | Account notes remain source-labelled and unverified | Sales operations | Map board columns and retention |
| P2 | Reorder interval definition | Uses only urbanXtracts order history; no retailer sell-through is inferred | Sales leadership | Choose average interval or days-since-last-order |
| Complete | MFA policy | TOTP is required for both workforce SSO and retailer password accounts; only `aal2` sessions reach profiles, capabilities, authenticated APIs, or RLS-protected data | Administration | Maintain administrator recovery procedures and test enrollment during account onboarding |
| P2 | Rep-held order approval | Disabled | Legal / CCO / Sales leadership | Approve written authority model or reject feature |
| P2 | Draft inventory holds | Drafts hold nothing; submit rechecks availability | Operations / Sales operations | Decide whether reservations ever occur before acceptance |
| P2 | Receiving claims | Retailer self-service claim workflow is not a production connector | Operations | Set time window, evidence, and disposition process |
| P2 | Notification channels and mandatory notices | The portal now prepares deduplicated invitation, incomplete-onboarding, license-expiry, and store-ready events, but every event remains `held_policy`; no unapproved external notice is sent | Sales leadership / CCO | Approve sender identity, templates, consent/mandatory events, escalation recipients, and delivery provider; then enable the sender and verify delivery/bounce handling |
| P2 | Document types and retention | Upload surfaces are not treated as a final records system | Operations / CCO | Define types, retention, and wind-down handling |
| P2 | Store API | No customer API credential is issued | Administration / Security | Define scope, credential lifecycle, and rate limits |
| P2 | Public COA resolver | Disabled; COAs remain authenticated | CCO / Security | Approve fields, retention, retest history, opaque codes, rate limit, and non-enumeration behavior |
| P2 | Public recall notice | Disabled; internal lot-impact analysis remains read-only | CCO | Approve trigger, wording, publication deadline, and acknowledgement model |
| Later | Payment collection | Balances, invoices, and payments are display-only | Finance / Legal / Security | Select processor and approve payment, refund, PCI, and dispute model |

## Release rule

A controlled deferral does not become a release blocker while its capability is absent from production navigation, cannot be triggered by an external user, and has a documented fail-closed or blank state. Enabling one requires its named decision, server-side implementation, authorization tests, audit coverage, and an update to the live readiness diagnostics.
